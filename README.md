# This tap has moved

`microclaw` formula has moved to:

https://github.com/microclaw/homebrew-tap

## Install from new tap

```bash
brew untap eevv/tap  # optional
brew tap microclaw/tap
brew install microclaw/tap/microclaw
```

## Note

If users run the old formula name, Homebrew will redirect via `tap_migrations.json`.
